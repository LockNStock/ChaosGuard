# 自动化求职评估与投递系统 - 技术交接手册 (v9.0 旗舰版)

## 1. 核心设计哲学 (Design Philosophy)
本系统采用“数据隔离”与“自适应调度”的双重架构。v9.0 引入了“任务 Profile 分离”机制，通过模拟人类在不同任务（如深度分析 vs. 快速抽提）下的心理节律差异，实现更高维度的行为指纹混淆。

---

## 2. 模块化工作流 (Modular Workflow)

### 2.1 职位战略评估 (Stage 1)
*   **核心脚本**：`gemini_job_evaluator.py`
*   **逻辑**：系统自动提取 `api_scouted_jobs.json` 中的 JD，通过专用评估引擎输出包含“匹配分、差距分析、战略匹配”的中文报告。

### 2.2 定制化简历生成 (Stage 2)
*   **核心脚本**：`gemini_web_analyzer.py`
*   **逻辑**：基于“单次对话隔离”原则，利用 `master_profile.json` 中的数据全量生成高度对齐的求职文档。

### 2.3 平台自动投递 (Stage 3)
*   **核心脚本**：`apply_roku_only.py`
*   **逻辑**：针对 Roku 平台进行表单映射，自动完成简历上传与基础信息填报。

---

## 3. ChaosGuard 混沌调度引擎 (v9.0)
系统核心调度已演进为基于 10,000 质数全域分布的“高反差/平滑双态”模型。

### 3.1 全域质数池 (10,000 Primes)
*   **熵源规模**：从 1,000 扩展至 **10,000**，拥有 **1,229 个** 质数基石。
*   **统计特征**：极大地丰富了长尾分布，最高延迟可达 2.7 小时，彻底模拟人类由于开会、进餐导致的长时间断开。

### 3.2 任务 Profile 双态选择 (Dual-Profile Logic)
系统支持根据任务性质选择不同的交互指纹：
*   **Smooth Profile (长线平滑)**：
    *   **适用**：Gemini 深度分析、多轮对话。
    *   **路径**：H (Hyper) → Y (Yield) → C (Cruise) → X (Chaos)。
    *   **特征**：渐进式降速，旨在维持任务连续性。
*   **Contrast Profile (短线高反差)**：
    *   **适用**：Gemini 快速抽提、短任务。
    *   **路径**：**H (Hyper) ⇋ X (Chaos)**。
    *   **特征**：禁用中间地带。风险一抬头即闪现至高延迟区，形成“断裂式”请求规律。

### 3.3 贝叶斯风险反馈与泊松采样
*   **感知层**：实时监听 Web 响应延迟，动态修正 Risk Level。
*   **平滑层**：通过泊松公式将离散质数转化为连续随机流，消除频率尖峰。

---

## 4. 战术呼吸感 (Tactical Breathing)
*   **Hyper 模式**：极速冲刺 (40-50 j/h)，用于安全期饱和作业。
*   **Yield 模式**：目标均衡 (18-20 j/h)，系统默认的主战区速度。
*   **Chaos 模式**：深度避战 (< 1 j/h)，用于环境恶劣时的自保。
*   **综合产出**：通过“安全期爆发+风险期消失”，实现 18 j/h 的高效吞吐。

---

## 5. 路线图 (Roadmap - v9 Done)
*   [x] **全域质数支持**：完成了 10,000 质数域的数学集成。
*   [x] **双态仿真对比**：通过 v9.0 Simulator 验证了 Smooth 与 Contrast 的行为差异。
*   [ ] **情绪引擎 (Next)**：模拟长时间操作后的“疲劳漂移”（速度随任务数线性递减）。

---

## 6. 数据集成与维护 (Data & Maintenance)
*   **唯一数据源**：`master_profile.json` 仍为系统最高真理。
*   **反馈闭环**：生产环境下需将每次 Web 响应时间通过 `observe_and_update` 回传给 Governor。

---

## 7. 操作流程 (Operation)
1. **环境准备**：启动 CDP 调试模式的 Chrome。
2. **Profile 选择**：根据当天任务组性质，在代码中初始化 `governor = ChaosGovernor(profile="contrast")` 或 `"smooth"`。
3. **注入与回传**：在主循环中调用 `get_next_delay()` 并及时回传 `actual_latency`。

---
> [!IMPORTANT]
> **版本说明**：v9.0 为现行最强抗检测版本，对于 Web Gemini 等高敏感接口，建议优先使用 **Contrast Profile**。
